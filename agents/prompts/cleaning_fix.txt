A pandas cleaning operation failed at runtime. Return corrected Python code only.

OPERATION ID: {operation_id}
OPERATION TYPE: {operation_type}
DESCRIPTION: {operation_description}

ORIGINAL CODE:
{original_code}

ERROR TRACEBACK:
{traceback}

INSTRUCTIONS:
- Fix the code so it runs without errors on a pandas DataFrame named `df`.
- Preserve the original intent of the operation.
- Do not import os, sys, subprocess, socket, requests, or urllib.
- Do not use open(), eval(), exec(), or __import__().
- Return ONLY the corrected Python code — no explanation, no markdown fences, no preamble.
- Start your response with the first line of code.
